name: demo_ui
description: MCP UI DSL widget showcase — reference implementation for all widget types
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  mcp_server:
    path: ../../../../../mcp_server/dart
