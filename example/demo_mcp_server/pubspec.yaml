name: demo_mcp_server
description: MCP server demo for Flutter MCP UI Runtime v1.0
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  mcp_server: ^1.0.1

dev_dependencies:
  lints: ^3.0.0